package store

const (
	memoryFilename = "memory.jsonl"
	indexFilename  = "index.json"
	schemaFilename = "schema.json"
	lockFilename   = ".lock"

	// ConfigFilename is the name of the per-project config file written by init.
	ConfigFilename = "config.yaml"
)
